// Solicita dois números inteiros ao usuário
var A = parseInt(prompt("Digite o primeiro número inteiro:"));
var B = parseInt(prompt("Digite o segundo número inteiro:"));

// Efetua a soma dos números e atribui o resultado à variável X
var X = A + B;

// Imprime o resultado em forma decimal
console.log("X = " + X.toFixed(2));
